---
/* Callout box. kind: info | warn | voice | alert. */
const { kind = 'info', title } = Astro.props;
---

<div class={`callout callout-${kind}`}>
  <div class='callout-title'>{title}</div>
  <slot />
</div>
