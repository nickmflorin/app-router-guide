---
/* A caption for a code snippet, the code counterpart of a diagram's
   figcaption: formal prose that explains what the snippet demonstrates.
   Place it inside a <Snippet>, immediately after the <pre class="code"> it
   describes; the Snippet container owns all spacing. Prefer
   moving summary-style comments OUT of the code and into one of these;
   in-code comments stay reserved for line-level explanation. */
---

<div class="code-caption">
  <slot />
</div>
