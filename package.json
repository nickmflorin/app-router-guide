{
  "name": "app-router-guide",
  "version": "0.1.0",
  "private": true,
  "description": "Craft Education — The App Router Guide (HTML document)",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.0"
  }
}
